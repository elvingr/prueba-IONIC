import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Practicas Tecnica LancerGroup',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
